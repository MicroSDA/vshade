# vshade
# ----------------Use for install vulkan ----------------
# sudo apt install libvulkan-dev
# sudo apt-get install libxcomposite-dev
# sudo apt install libxrandr-dev
# sudo apt install libxinerama-dev
# sudo apt install libxi-dev